{
  "name": "response",
  "version": "0.0.2",
  "author": "Coinxu",
  "main": "./dist/src",
  "typings": "./@types/src/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist && ./node_modules/.bin/tsc",
    "test": "npm run build && ./node_modules/.bin/mocha ./dist/__test__",
    "prepublish": "npm run test"
  },
  "dependencies": {
    "@types/node": "8.0.19",
    "@types/mocha": "2.2.41"
  },
  "devDependencies": {
    "typescript": "^2.4.2",
    "mocha": "*"
  }
}
